# 开发环境配置
spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
  datasource:
    username: root
    password: Gepoint
    url: jdbc:mysql://127.0.0.1:3306/frame?characterEncoding=utf8&useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource