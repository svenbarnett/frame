# 开发环境配置
logging:
  level:
    com.deepblue777.frame: debug
    tk.mybatis.mapper: debug
spring:
  datasource:
    username: root
    password: Gepoint
    url: jdbc:mysql://127.0.0.1:3306/frame?characterEncoding=utf8&useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
  redis:
    database: 12
    host: 127.0.0.1
    port: 6379
    password:
    timeout: 60
    jedis:
      pool:
        max-active: 8
        min-idle: 0
        max-idle: 8
        max-wait: -1
server:
  servlet:
    context-path: /test
